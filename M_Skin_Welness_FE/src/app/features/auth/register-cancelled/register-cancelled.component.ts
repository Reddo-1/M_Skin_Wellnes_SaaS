import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-register-cancelled',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './register-cancelled.component.html',
})
export class RegisterCancelledComponent {}
